package pieces;

import java.util.HashSet;
import java.util.Set;

import chess.Board;
import chess.Color;

public class Pawn extends Piece {
	
	public Pawn(int x, int y, Color team) {
		super(x, y, team);
		rank = "p";
	}
	
	public Set<List<Integer>> validMoves() {
		Set<int[]> moves = new HashSet<int[]>();

		if (!hasMoved) moves.add(new int[] {x, y + 2});
		if (y + 1 < Board.HEIGHT) moves.add(new int[] {x, y + 1});

		return moves;
	}
	
}
